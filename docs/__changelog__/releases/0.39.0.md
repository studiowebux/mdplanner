---
version: "0.39.0"
date: "2026-03-18T00:00:00Z"
---

### Added

- Settings page (v2): tabbed layout with five sections — Views, Project,
  Schedule, Tags, Links. All project.md fields are now editable from the UI.
- Settings Views tab: enabled/disabled collapsible groups, search filter,
  check all/uncheck all bulk actions.
- Settings dirty form tracking: snapshot-based comparison auto-detects when
  values revert to their original state. Dirty tabs show a warning dot and
  `beforeunload` prevents accidental navigation.
- OpenAPI (Scalar): vendored API reference at `/api/v1/reference` with
  documented schemas for all Settings, Milestone, and Portfolio endpoints.
- OpenAPI model names: all Zod schemas now register `.openapi("Name")` so
  they appear in the Swagger Models section (Task, FeaturesList,
  UpdateProjectConfig, ProjectLink, etc.).
- MCP server (v2): scaffold at `/mcp` with 7 milestone tools as thin
  wrappers over shared services. Service-first architecture with unified
  Zod schemas.
- Sidebar: dynamic navigation built from enabled features list. Toggling
  features in Settings immediately refreshes the sidebar.
- Eisenhower domain (v2): full stack — types, repository, service, API
  CRUD at `/api/v1/eisenhower`, SSR matrix view at `/eisenhower`, detail
  page, SSE live swap, and MCP tools (`list_eisenhower`, `get_eisenhower`,
  `create_eisenhower`, `update_eisenhower`, `delete_eisenhower`).
  Stored as markdown files in `eisenhower/` with quadrant arrays in
  frontmatter.
- Mindmap domain (v2): full stack — types, repository, service, API CRUD
  at `/api/v1/mindmaps`, SSR list view at `/mindmaps`, detail view with
  interactive SVG renderer (drag-and-drop, zoom/pan), inline edit, SSE
  refresh, and MCP tools (`list_mindmaps`, `get_mindmap`,
  `create_mindmap`, `update_mindmap`, `delete_mindmap`). Tree structure
  is encoded as indented markdown bullet lists in `mindmaps/`.
- Contacts domain (v2): full stack — types, repository, service, API CRUD
  at `/api/v1/contacts`, SSR list view at `/contacts` (card + table
  views, type/company filters, multi-field search), detail page, SSE
  live swap, and MCP tools (`list_contacts`, `get_contact`,
  `create_contact`, `update_contact`, `delete_contact`). Stored as
  markdown files in `contacts/` with type enum (lead, customer, partner,
  vendor, other), tags, and freeform notes.

### Added (patch)

- Quote Print / Save as PDF: quotes now have a print-only view at
  `GET /quotes/:id/print` and a "Print / Save as PDF" button on the quote
  detail page, restoring parity with invoices. Like invoices, it is a
  print-styled HTML page that auto-fires the browser's print dialog (no
  server-side PDF dependency). `QuotePrintView` mirrors `InvoicePrintView` —
  sender header, Bill-To, line items, totals, footer, notes — labelled QUOTE
  with Issued / Valid-until / Status meta. Both print views now share one
  auto-print script (`invoice-print.js` → renamed `billing-print.js`) and the
  same `invoice-print__*` print stylesheet; no second print stylesheet was
  forked.
- Bulk action bar: tag input with Add/Remove buttons — type a tag and press
  Enter (or click Add tag) to add it to all selected tasks; click Remove tag
  to strip it from all selected tasks.
- Bulk action bar: "Mark complete" button — sets every selected task to
  completed and moves it to Done in one action (`POST /tasks/batch-complete`).
  Distinct from "Move to…", which only changes the section; archived tasks are
  skipped and the action is idempotent for already-done tasks.
- Task list & board per-section pagination: each section (list) / column
  (board) now renders only `tasksPerSection` tasks (default 25) with a
  "Load more" control that fetches and appends the next chunk via
  `GET /tasks/more-section`, carrying the active filters/sort/search forward so
  the next page matches exactly what is shown. The page size is configurable in
  Settings → Project ("Tasks per section", stored as `tasks_per_section` in
  `project.md`). Keeps large boards/lists fast without dropping any tasks from
  view. The `/` and `/view` list endpoints share a single `loadFilteredItems`
  helper with the new paginator so filtering never drifts between them.
- Keyboard shortcut `t` toggles dark/light theme when no input is focused
  (mirrors existing button behavior).
- Analytics: global summary KPI strip above the filter bar — six cross-domain
  headline metrics (open tasks, active milestones, hours logged, revenue, open
  deals value, journal streak) that reflect the active filter range. Reuses the
  existing analytics aggregate; no new queries.
- Analytics: Tasks section now shows a by-priority bar chart (pure SVG, no
  libraries) above the drill-down tables, with an empty state when there are no
  tasks. Charts re-render on filter changes and follow the dark/light theme.
- Analytics: Goals section now shows a by-status bar chart above collapsed
  By Status / By Type drill-down tables, with an empty state when there are no
  goals.
- Analytics: Capacity Plans section now shows a per-plan utilization bar list
  (sorted high → low, color-coded by the shared utilization bands) with the
  full table moved into a collapsed drill-down. Utilization band thresholds are
  now a single shared helper used by both the analytics chart and the capacity
  plan detail bandwidth bars.
- Analytics: Time Tracking section now shows an hours-per-day line chart for the
  last 30 days (pure SVG, first line-chart consumer of the shared renderer),
  above By Person / By Project drill-down tables now collapsed by default. Days
  with no logged time render as zero so the line stays continuous; empty state
  when no hours are logged. Re-renders on filter changes and follows the
  dark/light theme.
- Analytics: Invoices section now shows a monthly revenue line chart for the
  last 12 months (revenue dated by invoice creation), above a By Status
  drill-down table now collapsed by default. Months with no revenue render as
  zero so the line stays continuous; hover shows the currency-formatted amount;
  empty state when there are no invoices. Re-renders on filter changes and
  follows the dark/light theme.
- Analytics: remaining domain sections now carry a chart and follow the
  canonical structure (stat strip → chart → collapsed drill-down tables), all
  pure SVG/CSS, theme-reactive, with per-section empty states:
  - Quotes — by-stage donut chart with legend.
  - Meetings — meetings-per-week bar chart (last 12 weeks).
  - Notes — by-type donut chart with legend.
  - Investors — target-amount-by-status bar chart (currency labels).
  - Finances — income-vs-expenses grouped bar chart (last 6 months).
  - Deals — by-stage funnel chart in the canonical deal-stage order.
  - Habits — current-month completion grid (one cell per habit per day).
  - Journal — 90-day entries heatmap (GitHub-style intensity grid).
  - Reflections — entries-per-month bar chart (last 12 months).
  Shared chart renderer gains donut, funnel and grouped-bar kinds; bar/line
  value labels and hovers use server-formatted display strings (currency,
  hours).

### Added (patch)

- Server shutdown notice: on graceful shutdown the server now publishes a
  `server.shutdown` SSE event (then a brief flush) before closing streams, and
  the UI shows a persistent "Server is shutting down — it will reconnect when
  it's back" toast that dismisses itself once the connection is re-established.
- Mindmap edit textarea: Tab indents (single line or every line in a
  multi-line selection); Shift+Tab outdents. 2-space step matches the
  bullet-tree format the parser/serializer require.
- Date range filter (all list views): every domain list page now shows a
  "Created after" / "Created before" date range filter in the collapsible
  filter panel, narrowing the list to records created within the range. The
  factory applies it universally — no per-domain configuration. Domains that
  filter on a domain-specific date (meetings, finance) keep their own field.
- Breadcrumb navigation (all detail pages): every detail page now renders a
  `<nav aria-label="breadcrumb">` at the top of `<main>` showing
  `Section / Record title`, where Section links back to the list view and
  the leaf is the current record (no link, `aria-current="page"`). Sits
  above the existing "Back" button. New shared component at
  `v2/components/ui/breadcrumb.tsx`, BEM-scoped CSS under `.breadcrumb*` in
  `components.css` using `--color-text-*` / `--color-accent` tokens.
  Applied across all 43 `*-detail.tsx` views (including note-detail's
  custom top-bar layout).
- Analytics jump bar (sticky): `/analytics` now renders a sticky pill-style
  jump bar between the filter row and the first section. One anchor per
  visible section from `ALL_SECTIONS` (hidden sections from the Customize
  panel are excluded). An `IntersectionObserver` highlights the active
  anchor as the user scrolls — `rootMargin: "-30% 0px -60% 0px"` keeps the
  active band stable across adjacent sections. Pure CSS pills + tokens
  from `variables.css`; no inline styles. Re-binds idempotently on
  `htmx:afterSettle` so filter / customize swaps don't drop the observer.
  New file `v2/static/js/analytics-jump-bar.js`; new component
  `AnalyticsJumpBar` in `v2/views/analytics.tsx`; CSS in
  `v2/static/css/views/analytics.css`.
- Finance Entries Chart view: new `?view=chart` mode showing (1) monthly
  income vs. expense as grouped server-rendered SVG bars and (2) a
  by-tag breakdown as horizontal SVG bars (one row per `(tag, type)`,
  sorted desc; untagged entries roll into an `uncategorized` bucket).
  Income uses `--color-success`, expense uses `--color-error`; all
  dimensions are SVG attributes (zero inline `style`, zero JS, fully
  CSP-safe). Two new static aggregators — `FinanceService.aggregateMonthly`
  and `FinanceService.aggregateByTag` — back the view; `getSummary()` now
  delegates the tag aggregation to the new helper (no behavior change).

### Fixed (patch)

- CSP violation: removed all htmx `hx-on:*` inline handlers, which the
  browser refused to run under our Content-Security-Policy (`new Function()` is
  blocked by `no unsafe-eval`), throwing `EvalError` and silently breaking the
  affected controls — most visibly the task comment form (its reset never ran).
  The reset / reload / redirect / re-trigger behaviors are now declared with
  `data-*` hooks (`data-reset-on-success`, `data-reload-on-success`,
  `data-redirect-on-success`, `data-trigger-on-success`, `data-remove-closest`)
  and handled by delegated listeners in `htmx-triggers.js`. Affected pages: task
  detail (comment, log-time, time-entry delete), capacity-plan detail
  (remove member/allocation), meeting detail (add action, add link), settings
  cache tab (rebuild buttons), and the shared archive/delete actions.
- Task list/board "Load more" button now reads the next chunk size (e.g.
  "Load 25 more") instead of the total tasks remaining in the section (e.g.
  "Load 387 more"), matching how many tasks one click actually loads. On the
  final page it shows the true remainder when it is smaller than the page size.
- Server shutdown no longer hangs on Ctrl+C. The graceful drain
  (`server.shutdown()`) waited for every connection to close, but long-lived
  `/sse` streams and MCP keep-alive connections never close on their own, so the
  process hung forever and had to be killed (then `AddrInUse` on the next boot).
  The drain is now bounded by a 1s budget (`runShutdown` in `utils/shutdown.ts`,
  raced against a timeout) so the server always exits promptly after the
  shutdown notice flushes and open streams are closed.
- Task detail description now renders Markdown as HTML (bold, lists, links,
  code) instead of showing the raw markup, while still linkifying @mentions,
  task ids, and commit hashes. Rendering goes through a sanitized renderer (raw
  HTML is dropped), so it is XSS-safe.
- Settings → Project: token / currency / locale / description fields can now be
  **cleared** — submitting an emptied field unsets it, instead of silently
  keeping the old value. The Cloudflare and GitHub token "clear" buttons now
  work. Numeric fields and the project name are unchanged.
- Task detail layout: the right-hand column no longer balloons to fill empty
  space (a stretched last section), the "Comment" button is its natural width
  instead of full-width, and the GitHub section's "Repo:" hint is legible.
- Task list: a very long task title is now truncated with an ellipsis instead
  of overflowing the row and pushing the metadata columns out of alignment.
- Task detail Assign field: typing a name without picking a person from the
  dropdown no longer leaves a stray, non-saving value — only a real selection
  is committed.
- Safari back/forward cache: returning to a page no longer logs an SSE
  connection error before reconnecting. Live update streams are now closed when
  the page is cached and re-established deterministically on restore.
- Topbar global filters (Project + Assignee): selecting more than one value
  applied only the **first** — the rest were silently dropped. The dropdowns
  gathered loose checkboxes via `hx-include`, which hits htmx #1541 (duplicate
  `name`s collapse to the first value in the request). Both dropdowns now live
  in one shared `<form>` so the browser serializes every checked box via
  FormData as repeated keys; the server already parsed those correctly. The two
  copy-pasted topbar blocks are now one reusable `GlobalFilterDropdown`
  component, and each dropdown gains a **search box**, **All/None** buttons, and
  a fixed-height scrollable list. Checked state is rendered server-side.
- Topbar global filters (Project + Assignee) QOL: (1) checked options now sort
  to the **top** of each dropdown list (a stable checked-first render order), so
  the active selection is visible without scrolling; (2) typing in the dropdown
  search no longer closes the panel — the search input was `type="search"`, and
  Safari ignores `autocomplete="off"` on it and shows a native history dropdown
  whose interaction tripped the outside-click close. The input is now
  `type="text"`, removing the native autocomplete entirely. Render-only change;
  search / All/None / badge wiring (`global-filter.js`) is untouched.
- Analytics milestone stats: the "done" count per milestone always reported `0`.
  The collector read a `doneCount` field that does not exist on the milestone
  (the enriched field is `completedCount`); a stale type cast hid the mismatch.
  It now maps `completedCount`, so milestone progress in Analytics reflects the
  real number of completed tasks.
- Note editor spacing: the add-block toolbar sat flush against the first block
  and each block's controls sat directly on its textarea. The toolbar is
  inserted before `.note-detail__body` (so the body's block `gap` never applied
  to it) and `.note-editor__block` had no styling. Added `margin-bottom` on the
  toolbar and made each editable block a `flex` column with a `gap`, removing the
  ad-hoc `padding-bottom` on the controls so spacing comes from one mechanism.
- Note editor: the per-block **Preview** button now renders into its preview
  pane. The edit-mode SSE guard attached an `htmx:beforeRequest` listener to the
  `<main>` SSE element that cancelled the request whenever `editing` was true —
  but `htmx:beforeRequest` bubbles, so it also cancelled the Preview button's
  own POST (the one case that only runs while editing). The guard now cancels
  only the SSE element's own live-refresh request (`e.target === sse`), letting
  descendant requests through while still protecting the open editor from
  SSE-driven re-render.
- Topbar dropdowns (project/assignee global filters, person switcher, search)
  could be painted over by a page's sticky table/list header (most visibly on
  the capacity-plans list). Two-part cause: the topbar was `static` with no
  stacking context, and — the flaky part — a `position: sticky` `<thead>` gets
  promoted to its own GPU/composited layer and could paint over the topbar
  dropdown even with a lower z-index, until some unrelated repaint corrected it
  (which is why opening the person menu first "fixed" the filter popup). The
  topbar now owns a stable stacking context at the new `--z-topbar` tier (100 —
  above every content sticky layer, below the mobile menu/overlay) AND is forced
  onto its own composited layer (`transform: translateZ(0)`) so the layer order
  is correct from first paint. Single shell-level fix; no per-table change.
- Topbar popups are now mutually exclusive: opening a global filter closes the
  person switcher and vice versa, so two topbar dropdowns can no longer be open
  at the same time.
- Mindmap layout: sibling branches no longer overlap when a node has 4 or more
  children. The renderer used two disagreeing vertical-spacing formulas — a
  leaf-span of `n*NODE_H + (n-1)*V_GAP` for centering a node vs a child-advance
  of `+V_GAP*2` per node when placing siblings — so a parent reserved less
  height than its children actually consumed. The `(n-1)*V_GAP` shortfall was
  hidden by `NODE_H` slack at 3 or fewer children and overlapped at 4+. Replaced
  both with a single per-leaf pitch `ROW_PITCH = NODE_H + V_GAP`, from which
  every subtree span and sibling advance is derived, so a node's center is
  always the exact middle of its band and the layout never under-reserves. The
  pure geometry was extracted to `mindmap-layout.js` (no DOM) and is now covered
  by unit tests (`tests/v2/mindmap-layout_test.ts`).

- Example demo data: repaired dangling cross-references so the Settings → Data
  Integrity scan reports 0 errors / 0 warnings (was 5 errors + 4 warnings).
  Brainstorm `linked_projects`/`linked_goals`/`linked_tasks` pointed at v1 IDs
  dropped during migration; two billing customers referenced companies absent
  from the demo. Repointed links to real entities and added the two missing
  company records.

- Global project/assignee filter: selecting more than one project (or
  assignee) now correctly shows the union of all selected (OR), instead of
  collapsing to just the first. The topbar fired a separate `change` POST per
  click with no request serialization, so two rapid selections raced — each
  wrote the full selection it knew at send time, and whichever response landed
  last won, often a stale single-item set. Both filter wraps now share one
  `hx-sync="closest .topbar__actions:queue last"` queue, so posts run
  sequentially and the latest full-state write always wins. Fixes every domain
  using the shared global filter (18 domains); the assignee filter, which had
  the same race, now holds reliably too.

- Analytics light-mode layout: the 13 `--analytics-*` layout tokens
  (`--analytics-chart-max-h`, `--analytics-stat-card-min-w`,
  `--analytics-kpi-card-min-w`, `--analytics-chart-label-size`,
  `--analytics-col-min-w`, `--analytics-util-bar-h`,
  `--analytics-by-value-min-w`, `--analytics-details-content-pad`, etc.)
  were defined only inside the `.dark {}` block in `variables.css`, so they
  resolved to nothing in light theme — consumers like
  `max-height: var(--analytics-chart-max-h)` silently dropped, leaving chart
  max-heights and stat-card min-widths unapplied in light mode. These are
  theme-independent dimensions; moved them into base `:root {}` alongside the
  already-correct `--analytics-progress-scroll-max-h`. CSS-only change.

- Deals pipeline view visual polish: the kanban-style board no longer
  reads as "amateur". Owner-pill ("Tommy") no longer stretches to card
  width — added `align-items: flex-start` on `.deal-pipeline__card-meta`
  so inline-block chips shrink to content. Tag chips dropped the border
  and use `--radius-sm` (vs assignee pill's `--radius-full`) so tags
  read as metadata, not as another person. Card value now uses
  `--color-accent` for the canonical "value" treatment matching analytics
  `StatCard`; currency suffix promoted from `xs/tertiary` to
  `sm/secondary`. Column header sits on its own `--color-bg-primary`
  band with rounded corners against the `--color-bg-secondary` column
  body, and the title bumps from `sm` to `base` for stronger hierarchy.
  Card hierarchy spacing: tight `--space-2xs` between related fields
  (title↔value, company↔contact); larger `--space-sm` for semantic
  breaks (value↔meta, meta↔tags). `.deal-pipeline` now uses
  `scrollbar-width: thin` + `scrollbar-gutter: stable` so the
  horizontal-scroll affordance is always visible. `--deal-pipeline-col-w`
  narrowed from 18rem → 16rem so more columns fit per viewport without
  scrolling. CSS-only change; zero TSX edits; all tokens from
  `variables.css`.
- Factory boolean toolbar toggles (`archived`, `showHidden`) now reset
  correctly on uncheck. HTML omits unchecked checkboxes from form
  submission; the factory middleware was falling back to the saved
  cookie value via `mergeParams`, so any boolean toggle stayed stuck
  "on" once enabled. The existing `hideCompleted` special case is now
  generalised to all three toolbar boolean keys (guarded per-domain on
  whether the toggle is rendered). `buildCanonicalUrl` now also omits
  `archived=false` / `showHidden=false` from the URL, matching the
  existing `hideCompleted` truthy-only convention. Affects every v2 list
  view that opts in to archive (45 domains) and the tasks domain
  showHidden toggle.
- Mindmap detail view: SVG renders immediately after Save — the htmx swap
  no longer leaves the canvas blank. `mindmap.js` now re-initializes on
  `htmx:afterSettle` (dedupe via `data-mindmap-initialized` flag on the
  container) and is loaded in edit mode so the post-save swap finds it.
- Milestone detail view: links and all other fields update live after editing
  via `SseRefresh` wired to `sse:milestone.updated`.
- UI state (global project/assignee filters, analytics hidden sections, task
  drag sort-reset) now persists in your **account** (`PersonPreferences.uiState`)
  instead of a single `ui_state` browser cookie. The cookie crammed every
  domain into one ~4KB blob and silently truncated once enough views had saved
  state — the root cause of filters/prefs intermittently failing to restore.
  State now survives a cookie clear, follows your identity across tabs and
  devices, and never truncates. The browser keeps only the small `mdp_identity`
  pointer cookie. Domain list-view filters were already account-backed via the
  factory; this brings the last cookie-based surfaces onto the same store, so
  all UI state is now consistent. Sidebar pins are now sourced solely from
  `pinnedNav` (one source of truth) instead of a parallel cookie copy.
- `parseFormBody` (`clearEmpty: true`) now passes `null` for empty fields so
  `mergeFields` applies the clear. Previously it passed `undefined`, which
  `mergeFields` silently skips — making it impossible to clear any optional
  field via edit forms across all domains.
- Identity select in topbar now vertically centers with `align-self: center`.
- Focused task row (`j`/`k` navigation) uses `box-shadow` instead of
  `outline` to avoid layout shift.
- Bulk action bar uses `overflow-x: auto` instead of `flex-wrap` so actions
  stay on one line on narrow viewports.
- SWOT quadrant header has `flex-shrink: 0`; quadrant list has
  `overflow-y: auto` to prevent overflow on small screens.
- Lean Canvas detail view: added edit mode (`?editing=true`) with per-item
  add/edit/remove on each of the 12 canvas sections, mirroring the Business
  Model Canvas pattern. The section textareas were removed from the create/
  edit sidenav — `parseFormBody` saved them as raw strings, which the
  repository serializer then iterated character-by-character, corrupting the
  saved data into single-character bullets.
- Fishbone detail view: added edit mode (`?editing=true`) with per-item
  add/edit/remove on each cause category, plus add/rename/delete of whole
  categories. Mirrors the SWOT quadrant-edit pattern. Previously the module
  was read-only after creation — causes could not be edited at all, and a
  newly created fishbone (which starts with zero categories) was unusable.
- Retrospective detail view: added edit mode (`?editing=true`) with per-item
  add/edit/remove on the Continue / Stop / Start sections. Those sections were
  removed from the create/edit sidenav — `parseFormBody` saved a textarea as a
  raw string which the repository serializer corrupted into single-character
  bullets (same root cause as Lean Canvas and Fishbone). Participants are now a
  People-linked autocomplete (`tags` + `source: "people"`) instead of a
  one-per-line textarea.
- Retrospective was unreadable after its first save (`GET /:id/edit` → 404,
  edit sidenav would not reopen). `RETROSPECTIVE_BODY_KEYS` listed `id`,
  `date`, and `status` as body keys, so `serialize()` wrote them to neither
  the frontmatter nor the body — `date`/`status` were lost on every update —
  and `parse()` then rejected the rewritten file because its guard required a
  frontmatter `id`/`title`. Body keys are now just `title` + the three
  sections; `parse()` also accepts a file identified by its `# heading`.
- SWOT, Lean Canvas, Business Model, and Retrospective carried four
  near-identical copies of the inline section-edit routes (detail page +
  add/edit/remove handlers). Consolidated into one `registerSectionEditRoutes`
  factory; SWOT and Retrospective now share a `QuadrantEditGrid` view
  component. The Lean Canvas / Business Model PUT handler also stopped
  returning a bare 204 — every mutation now re-renders the detail page.
- Reflection detail view: aligned to the canonical edit-mode pattern
  (`?editing=true`). Structured fields (title, period, date, template ID,
  tags) edit via the sidenav; the long-form `content` now edits in place via
  an "Edit Mode" toggle with an explicit Save button, instead of inside the
  sidenav. Mirrors the Journal reference implementation.
- Marketing Plan detail view: aligned to the canonical edit-mode pattern
  (`?editing=true`). Structured fields (name, status, budget, dates, project,
  responsible, team, audiences, channels, campaigns, hypothesis, learnings,
  linked goals) edit via the sidenav; the long-form `description` and `notes`
  now edit in place via an "Edit Mode" toggle with an explicit Save button per
  field (`PUT /marketing-plans/:id/description`, `PUT /marketing-plans/:id/notes`),
  instead of inside the dense sidenav form. Mirrors the Journal reference
  implementation.
- Brainstorm detail view: added edit mode (`?editing=true`). Each Q&A pair's
  question and answer now edit in place via the "Edit Mode" toggle with an
  explicit Save button per field (`PUT /brainstorms/:id/qa/:index`), instead of
  only through the sidenav. Previously the detail page had no edit affordance at
  all — answers could not be filled in or edited in place. The contenteditable +
  hidden-input + Save markup that 33 detail views each hand-rolled was extracted
  into a shared `InlineEditable` component (`views/components/inline-editable.tsx`);
  Brainstorm is its first consumer (the remaining 32 views migrate separately).
- Brief detail view: added edit mode (`?editing=true`) with per-item
  add/edit/remove on every section (Summary, Mission, the four RACI parties,
  High-Level Budget/Timeline, Culture, Change Capacity, Guiding Principles).
  Previously the brief had no in-place edit affordance at all — its long-form,
  multi-item sections could only be edited through the structured create/edit
  sidenav, which is the wrong surface for long-form content. Briefs now share
  the `registerSectionEditRoutes` factory and `QuadrantEditGrid` component with
  SWOT and Retrospective (no bespoke editor), with the four RACI parties grouped
  under a "RACI Matrix" heading.
- Edit Mode no longer autosaves: the quadrant/section array editors (SWOT,
  Retrospective, Eisenhower, MoSCoW, Project Value Board, Fishbone, Strategic
  Levels) previously persisted each item the moment you clicked away (blur),
  which silently saved half-typed edits. Each editable item now shows an
  explicit per-item Save (✓) button only while it is dirty; blur no longer
  saves, and the global unsaved-changes guard still warns on leave. Driven by
  the shared `quadrant-edit.js` (one change covers every quadrant view).
- Same explicit-Save conversion applied to the remaining autosave editors:
  Business Model and Lean Canvas section items (previously saved on `change`)
  and Quote line-item cells (previously saved on blur / Enter) now use the
  per-item ✓ Save button and no longer persist on blur. `EditModeToggle`'s
  doc comment, which still claimed "persists on blur", was corrected.
- Note title no longer silently autosaves. It previously persisted on every
  `change` (blur) via a standalone `hx-post`; it now follows the explicit-save
  model — a dirty-gated Save button appears next to the title only while it
  differs from the saved value, and the central dirty guard warns on leaving
  with the title unsaved (wired in `note-editor.js`). The note's project and
  the task assignee remain instant-apply-on-select: both are discrete
  autocomplete picks (not free-text typing), so applying the chosen value
  immediately is intentional, like sticky-note autosave. Capacity-plan
  detail has no inline autosave — its person/target autocompletes are fields
  inside an explicit "Add allocation/member" form submitted by a button.
- Onboarding detail view: steps can now be managed from the UI. Each step has
  a completion checkbox (toggles complete / not-started) and an inline-editable
  title with an explicit Save button; both persist via htmx POSTs that
  re-render the page and publish `onboarding.updated`. The progress bar now
  renders correctly with the native `<progress>` element — previously it used
  a `<div class="progress-bar__fill" data-pct>` markup that had no CSS or JS
  backing it, so the bar was always empty. Steps were otherwise read-only with
  no progress tracking.
- Portfolio detail view: surfaces three previously stored-but-invisible fields.
  `logo` now renders as a small image inside the title row; `billingCustomerId`
  resolves to the customer name (linked to `/customers/:id`, falls back to the
  raw ID if the customer was deleted); `brainManaged` shows as a Yes/No
  `<InfoItem>`. Both new info entries live in a guarded `.detail-info-row`
  matching the `company-detail` pattern. Detail route now fetches the customer
  alongside goals + people in a single `Promise.all`.
- Portfolio `client` field is now a Customer autocomplete (form +
  detail-page link) instead of free text. The create/edit form uses
  `AutocompleteWidget` against the `customers` source and stores the
  selected customer's ID. The detail page meta line resolves that ID via
  `getCustomerService().getById()` (in the same `Promise.all` as
  `billingCustomerId`) and renders the customer name as a link to
  `/customers/:id`; legacy free-text values (e.g. "Internal", "Multiple")
  still render as plain text when the value does not resolve to a
  customer. Schema unchanged (`client: string | null | undefined`) —
  accommodates both new IDs and legacy names without a migration.
- Business Model and Lean Canvas detail views (edit mode): each `.lc-section`
  is now bounded by `--lc-section-edit-max-h` (40vh) and its items list
  scrolls internally, so the "Add item" input stays anchored at the bottom
  of the section regardless of how many items the section holds. Previously
  long sections (20+ items) pushed the add input below the viewport with no
  way to scroll inside the panel. The rule is scoped via the canonical
  `[class*="-detail--editing"]` ancestor selector and only fires in edit
  mode; view mode is unchanged.
- Retrospective participants resolve to Person detail links more reliably:
  the name → Person lookup now tries exact match, then case-insensitive,
  then first-word match (so a participant entered as "Alice Martin" links
  to a Person record named "Alice" — the common drift between long-form
  participant names and short-name Person records). Ambiguous first-word
  matches fall back to plain text instead of mis-linking. The resolver lives
  in `v2/utils/person-name-match.ts` and is shared with the Person detail
  view (see next entry).
- Person detail page now lists every retrospective the person participated
  in under a new "Retrospectives" section (after Direct reports), each item
  a link to the retro with its date. Membership is determined by the same
  tolerant name matcher used on the retrospective detail page, so the two
  views agree on who is linked.
- Person detail page now shows an "Analytics" section above Assigned with
  five stat cards: Open tasks, Tasks done, Active goals, Time logged (sum
  of hours across all task time entries where `person === id`), and
  Meetings attended. Meeting membership is resolved through the same
  tolerant name matcher used for retrospectives (handles attendees stored
  as either person IDs or names). Goal counting matches the existing
  Assigned filter (owner equals person.name; status not success/failed).
  Stat-card BEM rules (`.person-detail__stat-grid`, `__stat-card`,
  `__stat-value`, `__stat-label`) live in `v2/static/css/views/people.css`
  and reuse the existing `--analytics-stat-card-min-w` /
  `--analytics-stat-value-size` tokens — promotion to a shared
  `.stat-card` component is a follow-up once the rule of three hits.
- Person detail page now lists the 10 most recent meetings the person
  attended in a new "Meetings" section between Retrospectives and
  Analytics, each item a link to `/meetings/:id` with the meeting date
  and attendee count. Attendance uses the same tolerant matcher as the
  Analytics meetings count (handles attendees stored as IDs, exact names,
  or short-name drift). When more than 10 attended meetings exist, a
  "View all (N)" link below the list opens `/meetings?q=<name>` — the
  meeting list's search predicate already covers the attendees array, so
  the search filter narrows to meetings featuring that person (with
  occasional false positives where the name appears in title / agenda /
  notes; acceptable until a dedicated attendee filter lands). Empty
  state ("No meetings attended yet.") via the shared `EmptyState`
  component.
- Person detail page now has a "Mentions" section above the audit footer
  that surfaces cross-domain references to the person by running the
  shared FTS `SearchEngine` against `person.name` and grouping results by
  entity type. Each group renders as a subsection ("Tasks (3)", "Notes
  (1)", etc.) with linked titles routing to the domain detail page via
  `ENTITY_TYPE_ROUTES`. The person's own person record is filtered out
  (their name matches their own FTS row); cross-person mentions are
  kept. Capped at 20 results; when the unfiltered match count exceeds
  20, a "Search all mentions (N)" link below the groups opens
  `/search?q=<name>`. Empty state via the shared `EmptyState`
  component. FTS match precision is documented as best-effort —
  common-name false positives are acceptable per the MVP scope.
- Onboarding: the record had no way to identify who is being onboarded, and
  steps had no way to record who owns each step — the module was unusable
  for real onboarding flows. The create/edit form now exposes the existing
  `personId` field as an "Onboardee" autocomplete (people source, stores
  ID) and ships a steps array-table where each row carries an "Owner"
  autocomplete. The detail page resolves both IDs to names and links to
  `/people/:id`. Step IDs and statuses round-trip through the form via
  hidden item-fields (new `hidden` variant in `ArrayTableItemField`), so
  the in-place checkbox/title editors keep working after a sidenav save.
- Note detail page: title was visually clipped to roughly 240 px. The header
  uses `flex-direction: column`, but `align-items: flex-start` was cascading
  in from `.detail-header` (components.css), so `.detail-title-row` hugged
  fit-content and the inline-edit `<input>` (with `width: 100%`) collapsed to
  its UA-default size. Added `align-items: stretch` to `.note-detail__header`
  so the title input fills the full header width.
- Note detail page: removed the always-on "Updated <X> ago" span from the
  header `.note-detail__meta`. It was a duplicate of the canonical
  `AuditMeta` footer (which already shows the updated timestamp) and gave
  the misleading impression of a permanent save indicator. Save confirmation
  is already covered by the global toast wired through `HX-Trigger` on
  `POST /notes/:id/title` and `POST /notes/:id/project`.
- Note editor: tab titles in existing tabs sections could not be renamed.
  Only freshly added tabs had a title input — pre-existing tabs rendered as
  static `<button>` text with no editor wiring, so `data-tab-panel-title`
  was never updated and `collectSection` kept reading the original value.
  `convertToEditable` now wires an inline `<input class="note-editor__tab-
  title-input">` into each existing tab bar button (mirrors the pattern
  used by `createTabElement` for new tabs), updates both
  `data-tab-title` on the button and `data-tab-panel-title` on the matching
  panel, and stops `mousedown`/`click` propagation so the tab-switch
  delegate ignores input interactions while the surrounding button still
  switches tabs on label click. Round-trip is unchanged on the server side
  (`### Tab: ${title}` + `<!-- tab-id: … -->` in `note.repository.ts` was
  already correct). The note editor's `PUT /notes/:id` save endpoint also
  now `publish("note.updated")` so other open clients refresh via SSE.
- Note editor: timeline items in existing timeline sections were read-only
  after creation — title, status, and date could not be changed without
  recreating the entry. Pre-existing timeline items rendered as static
  `<span>` elements by `note-blocks.tsx`, with no editor wiring; only items
  freshly added via "Add Item" got the title/status/date controls from
  `createTimelineItemElement`. `collectSection` therefore read the stale
  `data-timeline-title/status/date` attributes on save and round-tripped the
  original values. `convertToEditable` now calls a new
  `makeTimelineHeadersEditable(section)` that replaces each existing
  `.note-detail__timeline-header` content with the same title `<input>`,
  status `<select>`, date `<input type="date">`, and Del button used by
  `createTimelineItemElement`, syncing typing back to the item's data
  attributes and marking the editor dirty. Mirrors the tab-title-rename fix.
  Item content was already editable via the existing per-item raw-markdown
  textarea (`mergeSubBlocksToMarkdown`); parser/serializer in
  `note.repository.ts` already round-trips id/title/status/date unchanged.
- Settings → Navigation tab: the category quick-link jump bar now stays
  pinned to the top of the content area while scrolling a long category list,
  instead of scrolling away with the content. Matches the task-list sticky
  jump bar (`position: sticky` + opaque background + `--z-sticky`).
- Settings → Data: backup import is no longer one-click destructive. The
  Import form now runs a non-destructive **Preview** first (`POST
  /api/v1/backup/preview`): it parses the uploaded file without writing and
  shows the backup's version + creation date plus a per-domain table of how
  many records are New vs Overwrite (diffed by id against current data), with
  unknown/skipped domains flagged. Only an explicit "Confirm & Restore" then
  writes. Import remains upsert-only — records absent from the backup are
  never deleted, and this is now stated in the UI. The body reader and
  line-streaming parser were extracted to `v2/api/v1/backup/parser.ts` and
  are shared by both `/preview` and `/import`.
- Forms (global): textareas auto-grow to fit content. Added
  `field-sizing: content` to `.form__textarea` and `.settings-field__textarea`
  — the user no longer has to scroll inside a textarea. Matches the existing
  `.quadrant-card__textarea` pattern; native CSS, no JS shim.
- Sidenav: the form's Save button is now pinned sticky to the bottom of the
  sidenav body, so it stays visible no matter how tall the form is or how far
  the user has scrolled. Opaque background masks content scrolling
  underneath; negative horizontal margin extends the bar to the full panel
  width. When the form is dirty, the sidenav header gets an amber tint and
  the save-bar top border picks up the warning color — visible cues that
  unsaved changes are pending without requiring a scroll. `sidenav.js` now
  toggles an `is-dirty` class on the sidenav root alongside the existing
  `sidenav__title--dirty` bullet on the title.
- Settings → Shortcuts tab: restructured into two clearly labeled top-level
  sections — **View & UI Preferences** (Default views, Pinned navigation,
  Filter defaults) and **Keyboard Shortcuts** (Chord leader, Navigation keys).
  Each section ships an explicit "stored per user — applies to your account
  only" notice (`PersonPreferences` is server-side per actor, but the prior
  UI gave no indication of scope). The per-domain "Default views" selector
  now reads `DOMAIN_VIEW_MODES` from `v2/constants/mod.ts` and lists only
  the view modes that domain actually supports (sourced from each domain
  config's `extraViewModes` plus the factory's `grid` + `table` baseline).
  Domains with a single supported view are omitted from the selector
  entirely (previously every domain showed every possible view mode, so
  e.g. a Goals row offered Board/Timeline/Org options it cannot render).
- Settings → Data Integrity: the scan now reports per-domain breakdowns
  instead of a single flat table. Every scanned domain shows a green/red/
  yellow collapsible header with its checked count; clean domains collapse
  to a one-line summary, domains with issues expand to a table of severity/
  id/field/issue. The global "All clean" banner is shown only when every
  domain passes. Cross-reference validations added: portfolio.linkedGoals →
  goal IDs, meeting.relatedMeetings → meeting IDs, brainstorm.linkedTasks /
  linkedGoals / linkedProjects → task / goal IDs and portfolio names,
  reflection.templateId → reflection template IDs. Brainstorm, Reflection,
  and Reflection Template domains are now scanned.
- Currency display now respects the configured currency/locale on the
  Analytics dashboard and the Investor detail page. Both surfaces hardcoded a
  US `$` with fixed grouping/decimals (`analytics.tsx` carried a local
  `formatCurrency`; `investor-detail.tsx` inlined `` `$${…toLocaleString()}` ``),
  so they ignored the project's configured currency unlike the 18 other views
  that already use the shared locale-aware `utils/format.formatCurrency`. Both
  now call the shared helper (analytics keeps its 2-decimal display via
  `{ decimals: 2 }`); figures are unchanged for the default USD config and now
  follow the configured currency symbol/locale otherwise.
- Meeting detail attendees now link to the right person. Attendees are stored
  as person IDs (the attendees autocomplete uses `source: "people"`), but each
  pill rendered the raw ID and linked to `/people?q=<id>` — a people *search* by
  ID that lands on no/invalid match. Attendees now resolve (by ID, then via the
  tolerant name matcher for legacy free-text values) and link directly to
  `/people/:id` showing the person's name, matching how action-item owners
  already resolve. Unresolved values keep the search-by-name fallback. The
  attendees block was extracted to an exported `AttendeesSection` fragment and
  is now unit-tested. The list card and list-table attendee cells share the old
  `/people?q=` pattern and are tracked as a follow-up (they render through the
  factory without a resolved people map).
- Billing document header (invoice/quote print) logo `alt` text now reads
  "<Company> logo" instead of the bare company name, matching the portfolio
  logo alt convention. (Both image `alt` attributes already existed — an a11y
  audit grep had false-flagged them because the multi-line `<img>` tag carries
  `alt=` on the following line.)

### Changed

- Analytics page — professional UI pass. The page now reads as part of the
  product rather than a bespoke one-off: section panels adopt the canonical
  card surface (`--card-bg` / `--card-border` / `--card-radius` /
  `--card-padding` / `--card-shadow`, matching `.detail-section` /
  `.domain-card`) instead of the flat secondary-background blocks. The content
  area is a responsive dashboard **grid** — compact stat+chart sections
  (tasks, goals, quotes, meetings, customers, notes, investors, deals,
  reflections) flow into auto-fitting columns, while chart-heavy sections
  (milestones, capacity, time tracking, invoices, finances, habits, journal)
  span the full width via `.analytics__section--wide`. The CSS-only category
  tabs keep working because sections remain direct siblings of the tab radios.
  Redundant drill-down tables that merely mirrored their own chart were removed
  (Tasks "By Priority", Goals "By Status", Notes "By Type", Deals "By Stage");
  drill-downs that add information not in the chart (by-project, by-person,
  amount-by-status, by-type) are kept. The header timestamp is shorter and now
  uses the locale-aware `formatDate` helper (respecting the configured display
  locale) instead of a raw `toLocaleString()`. Filter and Customize changes show
  a slim indeterminate loading bar (`#analytics-loading`, an htmx
  `hx-indicator` that lives outside the swapped content so it survives the
  swap). New tokens `--analytics-section-min-w` / `--analytics-loading-h`; zero
  hardcoded values; charts and data contract unchanged.
- Edit-Mode inline fields (32 detail views): the hand-rolled contenteditable +
  hidden-input + Save-button triple that each view copied was replaced with the
  shared `InlineEditable` component (introduced with Brainstorm). All 32
  remaining views now consume it — billing-rate, brainstorm-template,
  business-model, company, contact, customer, deal, dns, eisenhower, finance,
  goal, habit, idea, investor, invoice, journal, marketing-plan, meeting,
  milestone, mindmap, moscow, onboarding, onboarding-template, payment,
  portfolio, project-value-board, quote, reflection, reflection-template, risk,
  safe, swot — eliminating ~30 lines of duplicated markup per block. Pure
  refactor: the component emits byte-identical markup (same ids, hx attributes,
  classes), so Edit-Mode save behavior is unchanged. The onboarding per-step
  inline title stays hand-rolled — it is a `<span>` widget posting via `hx-post`
  with a different DOM shape, not the canonical `.inline-editable` triple.
- Note editor block preview: the per-block **Preview** button is now a
  declarative htmx call (`hx-post /notes/preview-block`, `hx-include` the
  block's fields, `hx-swap` into a persistent preview pane) instead of a
  hand-rolled `fetch()`. The old `previewBlock()` JS is gone — the fetch, the
  manual `innerHTML`, the client-side code-block render, and the toggle-off
  branch. Each editable block now carries a `name="content"` textarea, a
  hidden `name="type"` field (kept in sync by Toggle), and an optional
  `name="lang"` input; `/preview-block` reads the form body and renders code
  blocks server-side (markdown for text). `htmx.process()` runs on
  dynamically built blocks (mirrors the existing field-swap wiring).
  Trade-off: clicking Preview a second time no longer hides the pane — it
  re-renders. Aligns with the htmx-only frontend-mutation decision; note
  autosave (`PUT /notes/:id`, JSON block model) stays a documented JS
  exception.
- Factory toolbar: the sort-reset control was a filled `btn--tertiary` pill
  labelled `✕ Sort` that looked like a one-off across every list view.
  Restyled to a ghost `btn--ghost btn--sm` pill labelled `↺ Clear sort`
  (with `aria-label`/`title`) so it matches the right-side toolbar
  vocabulary (view toggle, column toggle). Behavior unchanged — the
  button still GETs `/<domain>/view?sort=&order=` and only renders when
  `state.sort` is active.
- `UpdateProjectConfigSchema` now accepts all project.md fields (startDate,
  workingDaysPerWeek, workingDays, tags, links) instead of only name,
  description, and features.
- `ProjectService.updateConfig` applies all schema fields.
- `enabledFeatures` threaded through all view routes and layout components.
- Quote line items (draft quotes): the detail page now supports Excel-like
  inline editing. Click any description, quantity, or rate cell to edit it in
  place — saving on blur or Enter recalculates the line amount and quote totals
  server-side. An "Add row" button and per-row delete replace the
  open-the-sidenav round-trip. Editing is limited to `draft` quotes; sent or
  accepted quotes stay read-only. Type, unit, discount, and grouping remain
  editable via the sidenav form.
- Finance Entries: removed the Grid view. Finance data is inherently tabular,
  so the toolbar now shows only the Table button (and the future Chart button
  once that ticket lands). A new `DomainConfig.hideGridView` flag drives the
  suppression at the factory layer and coerces any stale `?view=grid` URL or
  cookie value to the Table render. The `FinanceCard` component was deleted
  as dead code.
- Meeting edit form: action-item Owner inputs now show the resolved person
  name in the search input (rather than the raw `person_…` ID) when editing
  an existing meeting. The hidden value still holds the person ID — save
  round-trips unchanged. Backed by a new
  `DomainConfig.resolveArrayDisplayValues` factory hook that threads per-row
  display overrides through `ArrayTable` → `ArrayTableRow` →
  `ArrayTableRowField` → `AutocompleteWidget`.
