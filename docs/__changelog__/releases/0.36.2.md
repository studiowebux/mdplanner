---
version: "0.36.2"
date: "2026-03-15T00:00:00Z"
---

### Added

- MCP tool `get_milestone_summary` returns tasks pre-grouped by section (In
  Progress, Pending Review, Todo, Done) with slim stubs (id, title, tags).
  Replaces `list_tasks { milestone }` plus manual grouping.
- REST endpoint `GET /milestones/{name}/summary` with optional `project` query
  parameter.
