services:
  mdplanner:
    image: studiowebux/mdplanner:latest
    container_name: mdplanner
    ports:
      - "8003:8003"
    volumes:
      - ./data:/data
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://127.0.0.1:8003/"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 15s
